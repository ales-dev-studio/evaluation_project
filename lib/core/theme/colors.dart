import 'package:flutter/material.dart';

class AppColors {
  // Private constructor to prevent instantiation of this class.
  AppColors._();

  // Primary color used across the app (dark blue).
  static const primaryColor = Color(0xff1F2A40);

  // Secondary color used for accents or highlights (purple).
  static const secondaryColor = Color(0xff6440FE);

  // Complementary color used for additional UI elements (golden brown).
  static const complementaryColor = Color(0xffAA935F);

  // Color for inactive icons (light gray).
  static const inactiveIconColor = Color(0xffC7CDD9);

  // Pure white color used for backgrounds or text.
  static const whiteColor = Color(0xffffffff);
}